# Технический долг и План рефакторинга

Данный документ описывает проблемы в кодовой базе, которые усложняют поддержку, расширение проекта или влияют на производительность.

## 🔴 Высокий приоритет (Critical)

### 1. Унификация именования и устранение "магических чисел"
*   **Описание:** Проблема: В коде присутствуют разрозненные числовые литералы (например, таймауты 500, 600, размеры 300, 200, отступы) и несистемные имена переменных.
*   **Влияние:** **Performance Heavy.** Создает спам событиями (60 раз в секунду). Все враги обрабатывают этот сигнал.
*   **Решение:**
    *   Провести ревизию всех числовых значений. Вынести таймауты, размеры, параметры.
    *   Стандартизировать именование пропсов компонентов, убрать разночтения, где это возможно без нарушения контрактов библиотек.

### 2. Обзор и устранение дублирования функционала (DRY)
*   **Описание:** Логика инициализации элементов встречается в нескольких местах 
*   **Решение:**
    1.  Провести аудит на предмет повторяющегося кода.
    2.  Реализовать абстракции которых Godot не хватает, в чстности унифицировать вализацию гомпонентов по готовности, если это лучшая практика.

---

## 🟡 Средний приоритет (Major)

### 3. Реструктуризация папок (Folder Structure Cleanup)
*   **Описание:** Некоторые папки находятся не на своих местах или содержат разнородные файлы.
*   **План действий:**
    *   `build/` (содержит Spawner) -> переместить в `components/spawners/`.
    *   `entities/items/Build/` (содержит Shop) -> переместить в `entities/buildings/`.
    *   `entities/items/` (содержит UI ресурсы) -> переместить `resources.tscn` и скрипты в `ui/hud/`.
    *   `entities/items/coin.tscn` -> переместить в `entities/collectables/`.
*   **Цель:** Четкое разделение: `components` (логика), `entities` (игровые объекты), `ui` (интерфейс).

### 4. Дублирование логики получения урона
*   **Описание:** Методы `take_damage` или логика в `on_hurt_box_entered` реализованы отдельно в каждом скрипте сущности.
*   **Решение:** Вынести логику здоровья и смерти в универсальный компонент `HealthComponent`.

### 5. Референс-код низкого качества (`_reference_code/`)
*   **Описание:** Прототип на Python (Pygame), находящийся в папке, написан плохо с точки зрения архитектуры.
*   **Влияние:** Может ввести разработчика в заблуждение при попытке портировать код напрямую.
*   **Решение:**
    *   Использовать папку **только** как справочник игровых механик (список оружия, типы врагов, баланс).
    *   Полностью переписать логику при переносе на GDScript, используя компонентный подход Godot.
    *   После завершения переноса механик — удалить папку.

---

## 🟢 Низкий приоритет (Minor)

### 6. Жесткие пути в `SceneManager`
*   **Описание:** Файл `global/scene_manager.gd` содержит строковые константы.
*   **Влияние:** Перемещение главных сцен требует обновления этого файла.
*   **Решение:** Рассмотреть использование `ResourceUID` для ссылок на сцены, чтобы пути были устойчивы к перемещению файлов.

### 7. Статическая типизация
*   **Описание:** Отсутствие строгой типизации в некоторых GDScript файлах.
*   **Решение:** Постепенно внедрять типы (`: float`, `: Vector2`, `-> void`) для улучшения автодополнения и отлова ошибок.